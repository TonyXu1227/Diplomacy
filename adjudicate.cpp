#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <map>
#include <stack>
#include <stdexcept>
#include <iomanip>
using namespace std;
#define min(a,b) (((a)<(b))?(a):(b))
#define max(a,b) (((a)>(b))?(a):(b))

int readOrders() {
    
    return 0;
}

int adjudicate() {
    if (true) {
        return 1;
    }
    return 0;
}

int main() {
    cout << "adjudicating...";
    int v = adjudicate();
    cout << "finished adjudication!";
    return 0;
}